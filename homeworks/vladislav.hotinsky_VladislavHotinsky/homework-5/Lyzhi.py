class skis(object):
	def __init__(self, name, model, slide)
	self.name = name
	self.model = model
	self.slide = slide
	
def mounting (self):
  return "%s %s" % (self.name, self.model)

#inheritance

from skis import skis

class skiing(skis):
	def __init__(self, name, model, strong_fastening, can_slide_mounting)
	self.strong_fastening = strong_fastening
	self.can_slide_mounting = can_slide_mounting

def strong_fastening(self):
	return self.strong_fastening
